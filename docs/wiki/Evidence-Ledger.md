# Evidence Ledger

This page distinguishes what the project has measured from what it merely expects.

## Stage history

| Date | Stage | Direct observation | Interpretation |
|---|---|---|---|
| Before 2026-07-24 | Stage 1 | Repeating the same readout produced max logit difference 0.0 | Reproducible, not evidence of information |
| 2026-07-24 | Stage 2, n=50 | Median Jacobian-versus-logit-lens top-10 Jaccard 0.194; random-vector specificity 1.00; shuffled-layer 0.22; mismatched-probe 0.40 against a 0.80 bar | Non-identical and non-noise, but structure-specificity failed; ambiguity |
| 2026-07-29 | Integration smoke attempt 1 | T4 15,360 MiB; identity checks passed; mixed NumPy import failure before model download | Fresh-process proof required after binary replacement |
| 2026-07-30 | Integration smoke attempt 2 | Same T4 class; fresh process and identity checks passed; Torchvision operator failure before model-weight load | Optional compiled dependency must be removed or matched |
| 2026-07-30 | Integration smoke attempt 3 | Pinned model and lens loaded; four layers probed; one 81-readout/64-crossing locus completed in 1.14064654s; peak allocation 4.074223 GiB | Bounded real-runtime compatibility established |
| 2026-07-31 | Stage 2b pilot, 20 prompts | 80 records; validator empty; sensitivity interactions positive at four layers; primary arithmetic coverage 2 against minimum 3 | Operational success, but robust result and threshold derivation undefined; no pilot decision |

## Current verified software evidence

The recovery worktree contains:

- pure dual-floor endpoint helpers;
- lossless 81-readout to 64-factorial materialization;
- fail-closed preflight registries and authorization guards;
- synthetic 8-by-8 harness;
- canonical excluded-input Colab smoke notebook;
- code-only deterministic bundle;
- aggregate validator with adversarial corruption tests.

Local tests demonstrate source and CPU behavior. The third smoke and pilot now
separately demonstrate bounded runtime and observed pilot behavior. They do not
demonstrate:

- a primary-floor Stage 2b effect under the preregistered coverage rule;
- derivable confirmation thresholds;
- a pilot pass/fail decision;
- confirmatory replication; or
- a functional, cognitive, global-workspace, or consciousness claim.

## Pilot evidence boundary

Observed:

- one Tesla T4 with 14.563 GiB VRAM;
- exact notebook, bundle, pilot-view, and authorization identities;
- 80 prompt-layer records across both floors and the full crossing;
- empty in-memory validator result and matched Colab-side SHA/cardinality audit;
- positive defined sensitivity-floor effects and interactions at all four layers;
- no holdout access and no raw prompt, activation, or full-logit persistence.

Not established:

- the primary floor's required category coverage;
- a robust Stage 2b result;
- threshold derivation or a pilot pass/fail decision;
- confirmation authorization or execution;
- artifact transfer.

## Unknowns that matter

1. Why the primary floor disproportionately lost arithmetic-completion prompts.
2. Whether a preregistered repair can improve category coverage without changing
   the estimand after seeing pilot results.
3. Whether an independently authorized repeat pilot is required before any new
   threshold derivation attempt.
4. Whether the positive sensitivity-floor interaction survives a future,
   separately authorized confirmation.
5. Whether the instrument can become stable enough for downstream J-space
   hypotheses.

Unknown means unmeasured, not unlikely.
