# Handoff: J-space lab, 2026-07-26

Written at the end of the session that ran the completed Stage 2 study through
the real EvoScientist engine. The pending task from the previous handoff is
**done**. This document records what happened, what it found, and what is open.

## 1. Status: the EvoScientist run is COMPLETE

Thread `019f9a2a-5b5a-7d13-80ea-11622e8de7a3`, model `gpt-5.6-sol` via ccproxy,
78 messages, status idle. All six acceptance criteria from
`INGOING_BRIEF_ARCHIMEDES_EVOSCIENTIST.md` section 7 are met.

Four project-scoped observations were written to
`~/.evoscientist/memories/observations/projects/P-a60fb67e40fc59be/`:

| Observation | Content |
|---|---|
| `O-a4298a4759650def` | Result is class-1 ambiguity: readout difference reported, matched-fit specificity not demonstrated |
| `O-9cfefddee56cd863` | Notebook underimplements the preregistered added-information and seed controls |
| `O-4b6c1f0c4a953c34` | jlens baseline paths need explicit float32 transport and CPU readout contracts (the two runtime bugs) |
| `O-3fc6372037a52415` | Next iteration should use held-out target-relative factorial controls, not the coupled D-margin rule |

EvoScientist **agreed** with the ambiguity decision but narrowed the reading: it
holds that "added information" should be restated as *reported non-identity*,
because a low Jaccard alone does not establish predictive, accurate, or useful
information. It also named cross-layer similarity, shared activation structure,
and D-metric saturation as live alternatives to the generic-transport account.

## 2. The important part: new defects it found

These are not in the original report. They affect whether H2 can be called a
pass, so they need triage before the study is treated as settled.

1. **The implemented H2 gate omits preregistered clauses.** `added_info_pass`
   checks only Jacobian-vs-logit median Jaccard and the Wilcoxon p-value. The
   proposal also required separation from the output and prompt-only baselines
   beyond rerun noise. Those clauses were never evaluated.
2. **Declared inference seed `[1]` was never executed.** The notebook declares
   seeds `[0,1]` but only sets seed 0.
3. **Specificity used one random-vector seed.** Three seeds `[0,1,2]` were
   computed; the specificity decision used seed 0 only.
4. **The D-margin rule couples non-redundancy and specificity** and may have
   insufficient headroom.
5. **The report does not expose all statistics needed to independently certify
   the gates**, and the primary artifacts were never transferred.

Open question for Dr. Mani: does finding 1 change the recorded decision? The
"added information PASS" rests on a gate that did not implement its own
preregistration. The overall decision was ambiguity either way, so no promotion
was gated on it, but the provenance record currently overstates what was tested.

## 3. Runtime work done this session (two bugs, both fixed)

### Bug A — 400 "requires a newer version of Codex" (self-inflicted, fixed)

Starting the stack with `--workdir /Volumes/Asylum/archimedes-wt-jspace` put that
worktree's own `EvoScientist/` package ahead of the editable install on
`sys.path`, because `langgraph_dev/manager.py:739` hardcodes `cwd=workspace_dir`.
The server therefore ran docs-branch code, which hardcodes
`_CODEX_CLIENT_VERSION = "0.142.5"`. Probe matrix: `gpt-5.6-sol` is rejected at
0.142.5, accepted at 0.144.1 and 0.145.0.

**Resolution:** workdir is now `/Volumes/Asylum/archimedes-wt-jspace/sakshi notes`.
It holds every study document, and contains no `EvoScientist/` package to shadow
with. Verified: imports resolve to `/Volumes/Asylum/archimedes/EvoScientist`,
client version 0.145.0.

**Keep this in mind:** never point `--workdir` at a directory that is itself a
checkout of this repo. cwd cannot be separated from workdir via the CLI.

### Bug B — silent empty answers on the Codex route (pre-existing, fixed)

ccproxy's `StreamingBufferService` drops the body when assembling a
**non-streaming** Responses reply at agent scale: the client receives
`status: completed` with an empty `output` array, which LangChain correctly
parses into `AIMessage(content=[])`. No error is raised anywhere. This is the
"empty-message bug" open since 2026-07-24.

Decisive evidence: a byte-identical captured 37-tool payload replayed through
ccproxy twice with only `stream` toggled — `false` gives `output_items: 0`,
`true` gives full SSE with the real text. Forcing Chat Completions is not an
escape; the same missing `output` surfaces there as a 502 "Failed to convert
provider response using format chain".

`models.py:547` did `kwargs.pop("streaming", None)` under a comment stating
streaming must stay ON. `pop` only removes an explicit value, so LangChain's
`streaming=False` default applied and requests went out `stream: false`.

**Shipped fix:** `EvoScientist/middleware/ccproxy_stream.py`, registered in
`EvoScientist.py` immediately *inside* the tool selector. It sets
`model_settings={"stream": True}` per call, for the ccproxy Codex Responses model
only. Per-call rather than per-instance is load-bearing: setting
`ChatOpenAI.streaming = True` would also reach the tool selector's
structured-output copy, which sets `disable_streaming=True` — that suppresses
LangChain's dispatch but `langchain_openai/chat_models/base.py:1374` still
serializes `stream: true` onto the wire, and the Responses branch of `_generate`
never pops it, so a `Stream` would be handed to code expecting a completed
`Response`.

Tests: `tests/test_ccproxy_stream_middleware.py`, 16 tests including
payload-level guards. Full suite **3058 passed, 12 skipped**.

**Not repaired:** the actual defect is server-side in ccproxy's non-streaming
assembly. It cannot be patched from EvoScientist — ccproxy runs as a separate OS
process (`ccproxy_manager.py:7`, "invoked via subprocess, not Python imports"),
so the client-side monkey-patches in `llm/patches.py` never reach it. Note that
`patches.py:139-157` is dead code for this reason and only handles
`output is None` anyway, not the observed `output == []`.

## 4. Repo state — UNCOMMITTED

Everything below is uncommitted on `main` in `/Volumes/Asylum/archimedes`.

New, from this session:
- `EvoScientist/middleware/ccproxy_stream.py`
- `tests/test_ccproxy_stream_middleware.py`

Modified, from this session:
- `EvoScientist/EvoScientist.py` (middleware import + registration)
- `EvoScientist/middleware/__init__.py` (export)

Modified, pre-existing from an EARLIER session — not mine, still undecided:
- `EvoScientist/config/settings.py` (`load_dotenv` override True to False)
- `EvoScientist/langgraph_dev/manager.py` (health timeout 60s to 300s)
- `tests/conftest.py`, `tests/test_config.py`,
  `tests/test_langgraph_dev_deploy_mode.py`, `tests/test_langgraph_manager.py`

The full suite is green with all of the above applied together.

## 5. Runtime state

- Stack up: ccproxy `:8000`, langgraph `:6174`, WebUI `:4716`, all 200.
- Workdir / cwd: `/Volumes/Asylum/archimedes-wt-jspace/sakshi notes`.
- Model `gpt-5.6-sol`, provider openai, reasoning effort high, via ccproxy Codex.
- Started with: `cd "/Volumes/Asylum/archimedes-wt-jspace/sakshi notes" && EvoSci --ui webui --workdir "/Volumes/Asylum/archimedes-wt-jspace/sakshi notes"`
- Cold start is ~70-120s. langgraph is launched `--no-reload`, so **restart it
  after any code change** or it keeps running the old module.
- `dangerous_mode: true` and `auto_approve: true` are still set in
  `~/.config/evoscientist/config.yaml` (lines 139, 136). These were left on
  deliberately — they are what make long autonomous runs work.
- **The upstream push path is hard-disabled** as of 2026-07-26:
  `git remote set-url --push upstream DISABLED` in the shared `.git/config`,
  covering the main tree and all four worktrees. `git push upstream ...` now
  fails with "correct access rights / repository exists" — that error is
  expected, not a credentials problem. Fetch and merge from upstream still work.
  Undo with
  `git remote set-url --push upstream https://github.com/EvoScientist/EvoScientist.git`.
- Audit of the 2026-07-25 ungated run: it stayed in bounds. Worktree HEAD
  unchanged at `364fc29`, no commits, no pushes (reflog last entry 7/24). Only
  untracked workspace writes: the agent's own report and this handoff.

## 6. What is open

1. **Triage the audit findings in section 2** — especially whether the H2 gate
   omission changes the recorded decision or just the provenance wording.
2. **Decide how to land the ccproxy streaming fix.** It is fork-internal work on
   `main`. Note `gh pr create` defaults to the public upstream, so pin
   `--repo bionicbutterfly13/EvoScientist --base main`.
3. **Consider an upstream ccproxy issue** for the non-streaming output loss. The
   middleware is a documented workaround, not a repair.
4. **Decide on the pre-existing uncommitted changes** in section 4.
5. Optionally disconnect the Colab T4 if still attached to the Google account.

## 7. Standing boundaries (unchanged)

Do not re-run the Colab experiment. Do not transfer or download the evidence
artifacts. Do not push to the public upstream. Do not promote any readout beyond
evidence class 1, and do not authorize Stage 3, publication, or Sakshi/Elume
integration — the decision was ambiguity, and this session's audit weakened
rather than strengthened the added-information leg.
